---

_kibana_default_config:
  csp:
    strict: true
  elasticsearch:
    hosts: "{{ kibana_elastic_hosts }}"
  server:
    host: "{{ kibana_host }}"
    port: "{{ kibana_port }}"
  telemetry:
    enabled: false
